> **DRAFT_ONLY. DO NOT SEND.**  
> 実在クライアント名禁止。金額・納期はプレースホルダ。エージェントは応募しない。収益・受注の保証は書かない。

# 04 — n8n：失敗通知と再実行の手順（CW / ランサーズ型）

| キー | 値 |
|---|---|
| pack | earn-jp-proposal-wave2-20260916 |
| id | JP-W2-04 |
| primary_desk | CrowdWorks / Lancers |
| also_paste | Coconala 見積もり返信 |
| offer | 既存 n8n が止まったとき、人へ戻し、同じ入力で再実行できる状態にする |
| draft | true |
| send | **禁止** |
| theme | n8n自動化 |
| chars_preview | 55 |
| chars_short | 405 |
| chars_standard | 682 |
| chars_long | 771 |

想定の募集（種類だけ）: すでにフローはあるが、失敗すると黙って止まる。通知先が無い。再実行の手順が属人的。

使わない道具: フロー全体の作り直しの強制、有料プラン加入の代行、鍵の預かり、ブラウザ操作。

---

## 画面の別欄（本文に埋め込まない）

| 欄 | 貼る値 |
|---|---|
| 希望報酬 / 提案金額 | `{{PRICE_YEN_DRAFT}}`（空なら応募しない） |
| 納期・完了予定 | `{{LEAD_TIME_DRAFT}}`（空なら応募しない） |
| 添付 | 公開 URL のみ。実行ログの全文は付けない |

手数料は `{{PLATFORM_FEE_NOTE}}`。公式ヘルプを直前に読む。率をここに固定しない。

---

## 一覧冒頭（先頭 50 字前後）

```
ご依頼の『{{JOB_TITLE}}』を、失敗したら人へ戻し、同じ入力で再実行できる n8n にする案です。
```

---

## 短文

```
ご依頼の『{{JOB_TITLE}}』を拝見しました。止まっても人が気づけて、同じ入力でやり直せる n8n の案です。黙って再送する仕組みにはしません。

{{DISPLAY_NAME}}です。ユタラボ（{{PORTFOLIO_URL}}）で、失敗したあとの戻し方を先に書く、という切り方を公開しています。既存のフローを壊さないことが先です。新しい有料プランの加入は、この下書きでは提案しません。

進め方は、(1)今の失敗の出方を依頼者の言葉で切る (2)通知と再実行を1本だけ足す (3)本番データを使わず確認する (4)鍵は依頼者画面のまま。ログ全文の提出は求めません。

ブラウザ自動操作はしません。未確認の稼働率は書きません。成果保証もしません。

納期の下書きは {{LEAD_TIME_DRAFT}}。希望額の下書きは {{PRICE_YEN_DRAFT}}。この文は下書きです。送信しません。
```

---

## 標準文

```
ご依頼の『{{JOB_TITLE}}』を拝見しました。{{SCOPE_ONE_LINER}}

{{DISPLAY_NAME}}です。ユタラボ（{{PORTFOLIO_URL}}）では、動いたように見えることより、止まったときに人が再開できることを先に見ます。今回は既存 n8n への失敗通知と再実行手順です。全面作り直しは、必要だと依頼者が言ったときだけ別見積もりにします。

【やり方】
1. 今どこで止まるか、誰が気づくか、同じ入力を再実行してよいかを先に合意する
2. 通知ノードと、人が押す再実行の手順を1本足す
3. ダミー失敗で、通知が届くことと二重送信しないことを確認する
4. 接続の鍵は依頼者側。こちらは手順だけ

【納品】
- 変更したシナリオ（依頼者テナント内）
- 失敗時の見方（秘密値はマスクした説明）
- 再実行手順
- 既存フローを壊していないかの検品リスト

【範囲外】
ブラウザ自動操作、スクレイピング、鍵の代理保管、黙っての再送、稼働率や節約額の保証、有料プラン加入の代行。

【確認したいこと】
- {{QUESTION_1}}
- {{QUESTION_2}}
- 失敗通知の宛先は1つか。営業時間外はどうするか

【連絡と修正】
テキスト中心です。着手は契約後。既存ノードを消す前に依頼者確認を挟みます。修正は合意した失敗経路の内側だけです。

希望額 {{PRICE_YEN_DRAFT}} / 完了目安 {{LEAD_TIME_DRAFT}} は画面の別欄です。未記入のまま送らないでください。この文は下書きです。
```

---

## 長文（ランサーズ 2000 字枠用）

```
ご依頼の『{{JOB_TITLE}}』を拝見しました。フローは既にあるが、失敗すると人が気づけない、という理解で進める案です。{{SCOPE_ONE_LINER}}

{{DISPLAY_NAME}}（ユタラボ {{PORTFOLIO_URL}}）です。公式の n8n 機能の範囲で、通知と再実行を足します。画面をクリックする自動化は対象外です。未確認の稼働率や「何分で直る」は書きません。収益の見通しも書きません。

【切り出し】
最近止まった例を、依頼者の言葉で3件まで聞きます。ログの全文や鍵は送らないでください。どのノードか、入力は再実行してよいか、二重送信が困るかだけを切る。個人情報がログに残るなら、保存場所を依頼者側に限定します。

【試作】
意図的に失敗するダミーを1本だけ使います。通知が届くこと。人が手順どおり再実行できること。成功済みの入力を勝手に再送しないことを見ます。本番の顧客宛に試験しません。

【引渡し】
依頼者のワークスペース上で動くこと。こちらに実行履歴を残しません。止めたときの戻し方と、触ってよいノードを手順メモに書きます。

【修正】
合意した失敗経路の内側だけです。別フローの作り直し、新しいアプリ接続、有料プランは別見積もりです。契約前の秘密ログ共有はお断りします。

【範囲外】
ブラウザ操作、スクレイピング、鍵の代理入力、自動再送、成果保証。

【確認したいこと】
- {{QUESTION_1}}
- {{QUESTION_2}}
- 再実行してよい入力と、人が中身を見てからにする入力の境はどこか

金額 {{PRICE_YEN_DRAFT}}、納期 {{LEAD_TIME_DRAFT}}。手数料表記は {{PLATFORM_FEE_NOTE}}。この段落は下書きであり送信しません。
```

---

## 他机への貼り替え

| 机 | 変更 |
|---|---|
| クラウドワークス | 短文または標準文 |
| ランサーズ | 標準文または長文 |
| ココナラ | 短文を見積もり返信へ |

---

## 架空の記入例（実在しない。学習用。送信禁止）

- ラベル: `依頼者I`（架空）
- タイトル例: `【架空】n8n が失敗しても気づけるようにし、同じ入力でやり直す`
- `{{SCOPE_ONE_LINER}}` 例: `既存フローを残したまま、失敗通知と人手の再実行を足す、という理解です。`
- `{{QUESTION_1}}` 例: `今はどこで失敗に気づいていますか。`
- `{{QUESTION_2}}` 例: `成功したあとに同じ入力が来たら無視してよいですか。`

実在の店名・団体名に置き換えない。

---

## STOP

- このファイルをコピーしただけでは応募完了にしない
- 実行ログ全文や鍵を git に残さない
- 空の金額・納期で送らない
- 黙っての再送を実装しない
