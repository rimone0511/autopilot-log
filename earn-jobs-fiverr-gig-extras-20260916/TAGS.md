# Search tags paste — unpublished Gig `build n8n automation with operator docs you can rerun`

JOBS PHASE. DRAFT_ONLY. English. Overview step only.

Fiverr help ([Creating a Gig](https://help.fiverr.com/hc/en-us/articles/360010451397-Creating-a-Gig)):

- Add up to **five tags** to improve visibility. Use all five.
- Add up to **five words or short phrases** that describe the Gig (metadata / context).
- Use short, relevant keywords clients are likely to search for.
- Changing tags later changes which searches you appear in. Batch with other extras; do not publish to “test rank”.

Conservative tag ceiling used here: 20 characters per tag (common create-Gig form; not stated in help). Spaces count. This pack: max 19.

Canonical set is the same five tags as PR #6. Do not replace them with `xAI`, `Grok`, `ChatGPT`, or competitor spam lists.

If the form has **only one** set of five slots, paste **Search tags** and skip metadata.

---

## Search tags (exactly 5)

Paste one per slot, in this order.

```
n8n automation
```

```
n8n workflow
```

```
workflow automation
```

```
api integration
```

```
operator docs
```

| Tag | Characters | Why it stays |
|---|---|---|
| n8n automation | 14 | Primary buyer query; already in the title |
| n8n workflow | 12 | Adjacent query; public gig chips use this phrasing |
| workflow automation | 19 | Category language (Automations & Workflows) |
| api integration | 15 | Honest scope: official APIs / webhooks only |
| operator docs | 13 | Differentiator already in the title; not an AI-agent claim |

Do not add a sixth tag. Do not duplicate the same phrase twice.

---

## Metadata phrases (only if Overview also asks)

Fiverr help: up to five words or short phrases that describe the Gig. These are **not** a second tag list to keyword-stuff.

```
n8n
```

```
business automation
```

```
webhook
```

```
sop writing
```

```
api workflow
```

| Phrase | Characters |
|---|---|
| n8n | 3 |
| business automation | 19 |
| webhook | 7 |
| sop writing | 11 |
| api workflow | 12 |

`sop writing` matches Premium “Workflow and SOP”. It is not a claim that this Gig is a writing / SEO content Gig.

---

## Service type / tool metadata (only if shown)

- Tool / platform: `n8n`
- Do not check Zapier or Make as the **product** of this Gig (migration is FAQ Q7 only)
- Do not check official-partner / Pro-only flags you do not have
- If a service-type dropdown exists under Automations & Workflows, pick the closest **workflow / automation** node. Do not pick AI image, video, or voice

---

## Fallback (only if a live slot rejects a tag)

Do not invent a new five-tag identity. If **one** tag is rejected by the form:

1. Keep the other four.
2. Replace only the rejected slot with `webhook automation` (19) if `operator docs` or `workflow automation` is the one that failed.
3. Stop. Do not loop synonyms.

Do not fall back to `ai agent`, `xai`, or `chatgpt`.

---

## Operator check

- [ ] Five search tags pasted, order above
- [ ] Each tag ≤ 20 characters
- [ ] Metadata pasted only if a second five-slot group exists
- [ ] No xAI / Grok / ChatGPT tags
- [ ] Title left unchanged
- [ ] Gig left **Draft** / unpublished
