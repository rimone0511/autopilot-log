# APPLY-WINDOW（骨格・下書き）並列キャップ

状態: **DRAFT / not production-applied / awaiting Pro PARALLEL-CAP-PERF**  
ロック日の目印: 2026-09-16 HANDS。数字の正は未着。

APPLY-WINDOW とは「次に本番へ焼くときの並列キャップの正」であり、この PR では窓の枠だけをファイルへ書いた。実行・配備・Bot 設定変更は含まない。

**最終数字を発明しない。** 下表の値はすべて `{{PRO_NUMBER}}` 系トークンのまま残す。

---

## 0. 待ち

| 待ち | 状態 | この窓でやってよいこと |
|---|---|---|
| Pro 案件 **PARALLEL-CAP-PERF** | 未回収 | 窓の枠とチェックリストだけ残す |
| MAIN Pro 枠 | 看板記録: 残り 0% | 再送結果を想像で埋めない |
| ソフト暫定 GO | ~10 + ~10（下記 §2） | 運用の仮上限として**言及だけ**。APPLY 値にしない |

トークンが残っている間は「適用済み」と書かない。

---

## 1. 窓に載せるつまみ（プレースホルダ）

Pro が埋める対象は次の 4 つ。HANDS 指定。足りない単位・TTL・料金は足さない。

| つまみ | APPLY 値（Pro 待ち） | すでに分かっていること（値ではない） | 発明しないこと |
|---|---|---|---|
| **max Cursor Grok** | `{{PRO_NUMBER_GROK_MAX}}` | 難所の実働候補（ORCH）。並行の上限は未観測 | ~10 を正にする。15 並行を証明済みと書く |
| **max SWE-2** | `{{PRO_NUMBER_SWE2_MAX}}` | 通常実働の第一候補（ORCH、〜2026-10-10）。並行の上限は未観測 | ~10 を正にする。キャンペーン限界費用と定額を混ぜる |
| **CU** | `{{PRO_NUMBER_CU}}` | 兄弟 APPLY が **mutex=1** をロック。並列 CU を焼かない | 1 より大きい数。実装ロックファイル名。待ち行列の長さ |
| **refill-on-empty** | `{{PRO_NUMBER_REFILL_ON_EMPTY}}` | 空席が出たときの補充。方針・個数とも未観測 | 自動補充の間隔、バースト、キュー深さ |

CU 行のトークンは「この窓への Pro スタンプ」用。兄弟パックの mutex=1 をこの PR で覆さない。Pro が 1 以外を返すまで、上げる案を書かない。

同一ファミリーの別名（単位が Pro 文面に出たときだけ使う。今は空のまま）:

- `{{PRO_NUMBER_GROK_MAX}}`
- `{{PRO_NUMBER_SWE2_MAX}}`
- `{{PRO_NUMBER_CU}}`
- `{{PRO_NUMBER_REFILL_ON_EMPTY}}`

本文中の総称は `{{PRO_NUMBER}}`。どれか一つでも残っていたら焼き込み未完。

---

## 2. ソフト暫定 GO（Pro までの仮。最終値ではない）

HANDS: ソフト暫定 GO は **Cursor Grok ~10 + SWE-2 ~10**。Pro `PARALLEL-CAP-PERF` まで。

これは:

- 最終キャップではない
- `{{PRO_NUMBER_GROK_MAX}}` / `{{PRO_NUMBER_SWE2_MAX}}` の代入値ではない
- 観測済み Effort でも、G1 の「15 並行の証明」でもない（兄弟 ORCH: G1 は 15 並行を証明しない）

Pro 回答が着くまでの運用メモにだけ使う。この窓の APPLY 列へ写さない。

---

## 3. 兄弟パックとの境界

`ops-bake-orch-cli-20260916/`（下書き）がロックしたことと矛盾させない。

| 兄弟側（ORCH / CLI / Head） | この窓 |
|---|---|
| 実働順: SWE-2 Max → Cursor Grok → Sol または親 Astra | 並行**数**だけ。振り分け順は再定義しない |
| CU mutex=1。並列 CU を焼かない | `{{PRO_NUMBER_CU}}` は上げない。CU を起動しない |
| Effort は requested / resolved / observed。未観測を 0 にしない | 未観測の並行数を 0 にも ~10 にもしない |
| G1 は 15 並行を証明しない | 15 をキャップ候補にしない |
| Hands~0 | 手元操作を増やさない。この PR で CU を起動しない |

---

## 4. refill-on-empty（枠だけ）

観測も Pro 回答も無いので、方針を書かない。空席補充の**欄**だけ置く。

Pro が返すまで空の質問（答えを先に書かない）:

1. 空席 1 つにつき補充する数は `{{PRO_NUMBER_REFILL_ON_EMPTY}}` か
2. Grok 枠と SWE-2 枠は別カウントか、合算か
3. CU 枠は補充対象か（mutex=1 と両立するか）

間隔・バースト・優先度は Pro 文面に数字が無いなら追加しない。

---

## 5. この下書きがやらないこと

- `{{PRO_NUMBER}}` を数字や「未設定=0」で置換する
- ~10+~10 を APPLY 値として焼く
- 振り分け表の新しい TTL・料金・並行数を足す
- bridge / 憲法 / スキル本体のパッチ
- 「適用済み」「実機合格」のスタンプ
- 秘密・Cookie・トークン・Drive ファイル ID の転記
