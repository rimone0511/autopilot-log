> DRAFT_ONLY. No X API. No posting. No likes/follows/DM. No real handles or full post text in git.

# Verify notes — X triage rubric

Mirror-bake: 2026-09-16.

## What this bake did **not** do

- No X MCP / API / Bearer / cookie.
- No Latest scrape.
- No hit counts.

Sibling measured query `len()` with `{{SINCE}}=2026-09-09` under 512. This bake does not re-claim a new max without re-running that count in SAMPLE-QUERIES.md — treat those queries as **paste drafts** and re-count if `{{SINCE}}` moves.

## Rubric (unchanged, fail-closed)

`keep_queue` needs **all four**: Buyer, Fit (KEYWORDS §0), Shape (contracting, not 正社員-only), Fresh (~7 days) **plus one concrete**. Else `park` or `skip`. Confused → `skip`. Reply from this pack: **never**.

## LIVE_DEVIN_STILL_NEEDED?

**Not as marketplace CU.** A Devin session that logs into X to mass-search would be the wrong control (scraping / engagement automation is out of scope for Autopilot Log and this earn-ops pack).

**Human:** paste SAMPLE-QUERIES into x.com Latest, score with RUBRIC, secret-free one-line logs. Do not buy X Premium from this pack’s instructions.

## Still skip (examples)

Seller gigs, 月収 bait, Greenhouse/Lever jobs, scrape/auto-like asks, crypto/adult, unpaid equity-only, KYC-proxy requests.
